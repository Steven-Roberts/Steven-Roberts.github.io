$(document).ready(function() {
    $('#title').typed({
        stringsElement: $('#typed-strings'),
        startDelay: 500,
        typeSpeed: 70,
        backSpeed: 10,
        backDelay: 1000
    });
});
